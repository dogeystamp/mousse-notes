[package]
name = "mousse-notes"
version = "0.1.1"
entrypoint = "lib.typ"
authors = ["dogeystamp <dogeystamp@disroot.org>"]
license = "MIT-0"
description = "Typeset notes, with a style inspired by LaTeX."
disciplines = ["computer-science", "mathematics"]
categories = ["book"]
compiler = "0.13.1"
exclude = ["thumbnail_pages.png"]

[template]
path = "template"
entrypoint = "main.typ"
thumbnail = "thumbnail.png"
